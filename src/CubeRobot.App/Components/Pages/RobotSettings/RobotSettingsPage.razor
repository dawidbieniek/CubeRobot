@page "/settings/robot"

<NavBar Title="Ustawienia robota" OnBackClicked="GoBackToHome" />

<div class="container p-2">
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col"></th>
                        <th scope="col">Chwytak</th>
                        <th scope="col">Ramię</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Przód</td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                    </tr>
                    <tr>
                        <td>Lewo</td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                    </tr>
                    <tr>
                        <td>Prawo</td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                    </tr>
                    <tr>
                        <td>Tył</td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                        <td><InputNumber @bind-Value="_accelerationValue" /></td>
                    </tr>
                </tbody>
            </table>
        </div> 
        <div class="col">
            <div class="row mb-2">
                <div style="max-width:300px;">
                    <CubeRobot.App.Components.Pages.Home.SerialPortSelector SelectionChanged="ChangeSerialPort"/>
                </div>
            </div>
            <div class="row">
                <div style="max-width:300px;">
                <button class="btn btn-primary" @onclick="SendSettings" >Aktualizuj ustawienia</button>
                </div>
            </div>
        </div>
    </div>

   
</div>

@code 
{
    [Inject]
    public NavigationManager Navigation { get; set; } = null!;

    private int _speedValue = 1000;
    private int _accelerationValue = 200;

    private void GoBackToHome()
    {
        Navigation.NavigateTo("/");
    }

    private void ChangeSerialPort(string portName)
    {
        
    }

    private void SendSettings()
    {
        
    }
}
